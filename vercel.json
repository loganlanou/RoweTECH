{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "curl -fsSL https://go.dev/dl/go1.23.4.linux-amd64.tar.gz | tar -C /vercel -xzf - && npm install",
  "buildCommand": "export PATH=$PATH:/vercel/go/bin && export GOPATH=/vercel/.go && go install github.com/a-h/templ/cmd/templ@latest && go install github.com/sqlc-dev/sqlc/cmd/sqlc@latest && export PATH=$PATH:$GOPATH/bin && make build-static",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ],
  "trailingSlash": true,
  "cleanUrls": true
}
